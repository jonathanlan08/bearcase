# HANDOFF

Last updated: 2026-09-06 (night). Built with Claude Fable 5.1. Phases 0 to 6 were a single session; phases 7, 9, and 10 ran as a lead plus parallel agents, each owning a file set (see "File ownership").

## State

| Phase | Status |
|---|---|
| 0 Recovery and planning | done — design docs (10), architecture, data model, trust boundary, implementation plan |
| 1 Foundation | done — monorepo, FastAPI + SQLAlchemy + Alembic (1 migration), Next.js 16 app, CI, Docker |
| 2 Synthetic data and engine | done — `northstar_facts.py` single source of truth, generator, ground truth, 16 formulas, scenario engine, tests with hand-checked values |
| 3 Processing and AI pipeline | done — validation, parsers, classification, evidence, mock + Anthropic providers, retrieval, guardrails, injection detection |
| 4 Claim Audit and Financial Verification | done — extraction, deterministic verification, add-back review, review decisions, audit events, UI |
| 5 Scenario Lab and report | done — immutable runs, sensitivity grid, report assembly, citation validation, MD/PDF export, UI |
| 5b Ask the deal | done — streaming chat (`chat_threads`, `chat_messages`, migration 3) with a tool-use loop over persisted rows for Anthropic and any OpenAI-compatible provider (`chat/providers.py` resolves the backend from keys in `.env`; `chat/openai_compat.py` runs the loop for Gemini, Groq, OpenRouter, Ollama, OpenAI, custom), a grounded no-tools fallback for models without tool calling, actionable provider errors with key redaction, and the rule-based composer when no key is present. The assistant is general-purpose and also knows the deal: general questions are answered in Markdown (rendered with citation chips inline); deal facts come only from the eight tools and carry `[E:…]`/`[M:…]` markers checked in code; `stream_reply` labels every reply with a scope (`deal` when a citation resolved or a tool ran, else `general`) stored in the citations payload and sent in the `citations` and `done` events; `POST /chat` accepts an optional `model` (regex-validated, 400 otherwise, applied only when the backend is live, recorded on the rows) and `GET /chat/config` returns a per-provider `models` list; the panel has a model picker when live, copy and regenerate actions, and a scope label in the footer. Live models remain unverified end to end here (no key) |
| 6c Scene realism | done — transmission glass core with lit interior, paper with thickness and shading, procedural environment and contact shadows, soft additive particles, bloom and vignette by tier, camera dolly; both waterfall charts rebuilt as truncated-axis bridges |
| 6b Design pass | done — removed serif display, eyebrows, section numbers, dot strips; one radius scale; landing sections use five distinct layouts; impeccable detector reports 0 findings |
| 6 Landing page and polish | done — Ink/Paper design, Evidence Sculpture scene (layered sheets, glass, red link, bear) with static fallback, methodology page |
| 7 Astra review response | done — an independent review (`bearcase.ai astra rewrite/audit/REVIEW.md`, untracked, read-only) was answered item by item in `docs/astra-review-response.md`. Adopted: per-visitor demo isolation with 14-day retention and legacy shared sessions retired; bounded upload reads, an XLSX archive budget, storage quotas (50 documents per deal, 500 MB per user), and a per-user token-bucket rate limit on demo start, upload, reprocess, process, chat, and ask (`api/ratelimit.py`); `P&L` sheet aliases in the statement mapper; the stray `fmtValue` page export removed; findings rewritten for the buyer with one engine formatter for every reader-facing number; overview start section with a plain next step and a two-term glossary; claim page with "Confirm assessment", "Seller says / Documents show", a confidence explanation, and "Ask about this claim"; citation viewer as one aligned table per sheet with separators; financials led by "What changed and why"; scenario lab with a sticky result strip and per-assumption help; chat panel at `min(80vw, 880px)` with four quick prompts, claim context through `lib/chat-bus.ts`, and "same metric" markers; the hero told in three chapters (seller says 18%, statements show 11.6%, open the evidence) with visible captions and a pause control; landing page framing section with the four-step first session and "Open your workspace"; methodology retitled "How we check claims" with "What a citation means"; honest citation wording (provenance, not truth; a deal reply needs at least one resolved citation to be grounded; the fictional-data note goes into the prompt only for demo deals); README, SECURITY, trust boundary, product spec, and both design specs updated. Also in this batch: findings and report prose rewritten for the buyer with a prose test; Deal Room as what you uploaded / what we understood / what is missing; audit history in sentences with the viewer's zone named; new-deal form that refuses blank required numbers; report page with a 76-character column and chips that open the source; mobile nav labels and chat trigger clear of the bottom bar. Deferred with reasons in the response doc: external penetration test, five-user study, CVE scan, Docker topology run, unfamiliar-document validation, live-model injection measurement, the VoiceOver pass, chip size, a shortcuts preference |
| 8 Production pass | done — `BEARCASE_ENV=production` refuses the development `BEARCASE_SECRET_KEY` and logs (never raises for) warnings on a localhost CORS origin, SQLite, local storage, `auto` chat with no key, a disabled limiter, and a short secret (`config.py`: `_refuse_unsafe_production`, `production_warnings`, `has_chat_credentials`); `postgres://` and `postgresql://` URLs are normalised to `postgresql+psycopg://` so a hosted database URL works with the installed driver; `bearcase doctor [--json]` prints a readiness table (env, database reachable and at head, storage probe written and read back, secret set, chat backend and extraction provider by label and model only, rate limits, quotas, CORS, job runner) and exits 1 on any failure, resolving the chat backend from settings alone (no provider call); `bearcase migrate` and doctor find `alembic.ini` in the checkout or in the working directory, so they work in the Docker image where the package is installed into site-packages; the API startup line names env, db, storage, jobs, extraction, chat backend, and limiter; `infra/render.yaml` is a Render Blueprint (free Postgres, Docker web service from `infra/api.Dockerfile`, generated secret, database URL wired, `GEMINI_API_KEY` unsynced, CORS and proxy-header settings, `migrate && doctor && serve` as the start command, commented S3 and disk blocks, a one-instance note for the in-memory limiter); no `apps/web/vercel.json` because the Next.js rewrite already covers `/api` (Vercel needs the root directory, `npm ci --legacy-peer-deps`, and `BEARCASE_API_URL`); `docs/deployment.md` rewritten (path table, migrate-then-doctor, Render + Vercel steps, Compose, env var table, Gemini free-tier caveats with the measured 25 to 40 s answers, the 503 retries and the 429, what the chat does about them, what a paid product still needs); README nav link, Deploy section, and the shared-quota sentence; SECURITY production-mode control row and an eight-step production checklist, the stale "does not refuse the default secret" gap removed; `.env.example` gained the chat-tuning and production lines. Not run on Render or Vercel from this machine; the Compose file's development mode still auto-migrates through `app.run_migrations`, which assumes the checkout layout |
| 9 First-customer workflow | done — the product is framed for one buyer and one workflow, following the owner's advice (be specific about whose problem it solves, one promise sentence, one excellent workflow with the chat helping to finish it, trust as part of the product, an early bounded paid pilot with cost tracking, an advantage built from reviewed work, and a measure of whether users can identify and explain a discrepancy without help). Promise: "BearCase checks a seller's documents for financial inconsistencies and shows you what to investigate before buying the business." First customer: a person buying their first small business with an accountant or adviser; small acquisition teams second. Workflow: upload documents, see important discrepancies, inspect evidence, export questions for the seller. Surfaces (shared contract, one owner per file set): `GET /api/deals/{id}/seller-questions` built by one deterministic function in `reports/assemble.py` that the report's "Management questions" section also uses, with `/export?format=md` or `txt` (Content-Disposition, audit `seller_questions.exported` with the count) and the page `/app/deals/[dealId]/questions` whose cards offer "Explain this finding" (prefill) and "Draft a question for the seller about this" (send, citing evidence ids) through `lib/chat-bus.ts`; `DELETE /api/deals/{id}/documents/{document_id}` (204, owner-scoped, removes versions, files, evidence, and the claims sourced from the document with their links and decisions, audit `document.deleted`, `X-BearCase-Reanalyse: true` makes the UI offer "Run analysis"); `GET /api/deals/{id}/progress` (documents, findings, evidence, questions; evidence is done by the new `evidence.opened` event written by the evidence route, one row per evidence per user per day) behind a progress strip on the overview; `GET /api/deals/{id}/usage` (chat messages, tokens, tool calls, a count per model, documents, storage bytes, `cost_estimate_usd` from the per-model price table in `chat/providers.py`, `pricing_note`) behind a usage panel on the audit page; `GET /api/deals/{id}/review-dataset` (JSONL, owner only, audit `review_dataset.exported`) with `bearcase export-reviews --deal <id> --out file.jsonl` and `bearcase eval --reviews file.jsonl` printing agreement between `ai_status` and `reviewer_status` overall and per claim type plus the count of corrections; the landing page `/trust` ("How BearCase handles your documents": storage, deletion, retention, which provider sees what, and source facts versus calculations versus assumptions versus AI interpretations, with the line that a resolving citation does not establish that an answer is correct). Documents: `docs/go-to-market.md` (promise, first-customer hypothesis and how to tell the segments apart, 30-day plan with targets and what counts as evidence, interview script to run before the demo, usability test protocol logged with the audit history and the progress strip, bounded pilot offer with two price hypotheses, cost tracking with a unit-economics template, the reviewed-work flywheel, risks and trust commitments, a pitch outline), `docs/evaluation.md` (regression suite, review-dataset export, agreement metric, release gate and release record), and the README, product spec, application spec, and SECURITY rows for the new surfaces. Not done: the conversations, pilots, and paid pilot themselves (the plan has targets, no results); a per-deal permission field for the reviewed set; automatic retention for non-demo uploads; account or deal deletion |
| 10 Accounts, sharing, billing | done — the owner's "ok do all of it" on the list: accounts with email verification and password reset, deal sharing with collaborators, a per-user chat budget, Stripe Checkout for the bounded pilot, and terms and privacy pages. Settings (`config.py`): `email_provider` (`console` or `resend`), `email_from`, `resend_api_key`, `app_base_url` (the origin every emailed link is built from), `chat_monthly_request_limit` (300), `stripe_secret_key`, `stripe_webhook_secret`, `pilot_price_cents` (50000), `pilot_currency` (`usd`). One migration adds `users.email_verified_at`, `auth_tokens` (purpose `verify`/`reset`/`invite`, SHA-256 `token_hash`, `expires_at`, `used_at`, JSON `payload`), `deal_members` (`user_id` null until accepted, `invited_email`, role `viewer`/`editor`, `invited_by`, `accepted_at`), and `purchases` (kind `pilot`, `amount_cents`, `currency`, status `pending`/`paid`/`failed`, unique `stripe_session_id`, `stripe_payment_intent`, optional `deal_id`, `paid_at`). Auth (`api/routes/auth.py`): register sends a verification link (`/verify?token=`, 24 h, single use), `verify`, `resend-verification`, `request-reset` (always 200, `/reset?token=`, 1 h), `reset` (revokes every session of the user), `me.email_verified`; all rate-limited by client address like login. Email adapter `bearcase/email/`: `Emailer` protocol, `ConsoleEmailer` (logs, keeps the last 50 for tests), `ResendEmailer` (httpx to api.resend.com), `get_emailer()`. Sharing: `GET/POST /api/deals/{id}/members` (owner only, verified address, five members, invite email to `/invite?token=`, pending membership when the address already has an account), `POST /api/invites/accept` (signed in, email must match case-insensitively), `DELETE .../members/{member_id}` (owner or the member); `get_deal` admits the owner or an accepted member and roles are checked per action (editors: everything but delete the deal, manage members, export the review dataset; viewers: read, chat, export seller questions); audit rows carry the member's user id. Chat budget: assistant `chat_messages` this calendar month (UTC) across all threads of the user, demo visitors included; at the limit the chat route answers 429 with the reset date and `chat/config` carries `budget: {limit, used, resets_on}`. Billing (`api/routes/billing.py`): `status` (configured, pilot offer, purchases), `checkout` (Stripe Checkout Session with inline price data, success and cancel URLs on `/pilot`, a pending purchase; 503 unconfigured), `webhook` (signature verified with the endpoint secret, `checkout.session.completed` marks the purchase paid, idempotent); the `stripe` SDK sits behind a small adapter with a fake for tests. Web: `/verify`, `/reset`, `/forgot`, `/invite`, `/pilot`, `/terms`, `/privacy`; the Share panel from the deal overview header; an unverified-account banner with resend in the app shell; the chat empty state and footer show the budget and the 429 message; hero and footer link to `/pilot`, `/terms`, `/privacy`. Docs: README section and env lines, `docs/deployment.md` (Resend, Stripe keys and webhook URL, `app_base_url`, the table), SECURITY rows and gaps, CLAUDE invariant 5, `docs/data-model.md`, `docs/go-to-market.md` (checkout in the pilot offer), `.env.example`. Not done: Resend and Stripe were not called from this machine (adapters have console and fake implementations, exercised by tests only); no account deletion, owner transfer, refunds, invoices, or subscriptions; the monthly team plan is not sold through the app |

## Verification

The lead runs the checks once at the end of the batch (parallel agents share `.next`, so `next build` and Playwright run only then) and fills this table.

| Check | Result |
|---|---|
| `pytest` (apps/api) | 218 passed (accounts, sharing, budget, billing, audit regressions, prose, chat, engine, ingest, API) |
| `bearcase eval` (mock provider) | PASS, 12/12 checks; extraction recall 19/19; status accuracy 19/19; 58/58 citations resolve; report v1 valid (36/36 material statements cited); Ask the Deal grounding ok |
| `ruff check` / `ruff format --check` / `mypy src` | clean (82 files); `bearcase migrate` then `bearcase doctor` report 0 failures |
| `vitest` (apps/web) | 90 passed |
| `tsc --noEmit` / `eslint` | clean |
| `next build` | success |
| Playwright smoke (chromium + mobile) | 6 passed |
| Browser console on landing and app pages | no errors (only the Three.js Clock deprecation warning); scene-check ok incl. pause/resume |
| Screenshots | `docs/screenshots/01-15` recaptured from the running stack after phase 9 (14 seller questions, 15 trust page) |

Live model: Google Gemini (gemini-3.8-flash, free tier) was exercised end to end on 2026-09-06 with the owner's key in `.env`: the chat resolved the backend, ran four tools, and returned a grounded, cited answer; a 503 "high demand" from the provider is mapped to a plain retry message. The Anthropic provider and the other OpenAI-compatible providers are covered by fake-client tests only. Docker Compose was written but not run (no Docker on this machine).

## Decisions that differ from the original package (deliberate)

1. **Zero-infrastructure default.** SQLite + local storage + in-process job runner so a clean checkout runs with `make seed && make dev`. PostgreSQL, S3/MinIO, and a polling worker are config switches (Docker Compose shows the full shape). Reason: the dev machine had no Docker; a portfolio demo must actually run.
2. **No pgvector.** The corpus is small and verification needs exact lexical/numeric overlap; a BM25 retriever sits behind a `Retriever` protocol.
3. **Numeric claims are settled by code, not the model**, in both providers. The model only judges narrative claims and drafts prose.
4. **Add-back review is rule-based against statement lines** (recurrence, prior-year excess, missing line) with human override that recomputes verified EBITDA. The rules do not establish a market salary or match a settlement; the trust boundary says so.
5. **The mock provider is a real rule-based extractor** over document text, not canned output; the same fixtures exercise the real parsing path.
6. **Chat `auto` mode connects the first key it finds.** Astra's copy required a provider to be named before any live call. `auto` stayed because the free-provider path depends on it; it is documented, `mock` pins the chat offline, and the panel always names the provider that answered.
7. **Astra's repairs were ported, not copied.** Its copy predates the multi-provider chat; each idea was re-implemented against the current tree, and its citation rule was narrowed to deal-scope replies so general answers are not flagged.

## File ownership

Phase 7 partition: `api/routes/demo.py` + `auth.py` (isolation), `ingest/validation.py` + `api/routes/documents.py` + `config.py` (limits), `chat/` + `components/domain/deal-chat.tsx` (chat wording and panel), `app/app/deals/[dealId]/page.tsx` (overview), `components/landing/` + `components/scene/` (hero), `ingest/statement_mapper.py` (mapping), documentation (`README.md`, `SECURITY.md`, `HANDOFF.md`, `docs/`). Phase 9 partition: seller questions (`reports/assemble.py`, its route, the questions page), document deletion (`api/routes/documents.py`, the Deal Room), progress and usage (their routes, the overview strip, the audit page panel, `chat/providers.py` price table), review dataset (its route, `cli.py`, `evals/`), the `/trust` landing page and chat actions, and documentation (`README.md`, `SECURITY.md`, `HANDOFF.md`, `docs/go-to-market.md`, `docs/evaluation.md`, `docs/product-spec.md`, `docs/design/application-spec.md`). Phase 10 partition: migration + models + `config.py`, `api/routes/auth.py` + `bearcase/email/`, sharing (`api/deps.py`, the members and invites routes, role checks in the deal routes), chat budget (`chat/` routes and config), billing (`api/routes/billing.py` and the Stripe adapter), web auth pages (`/verify`, `/reset`, `/forgot`, `/invite`, the shell banner), web sharing and pilot (`/pilot`, `/terms`, `/privacy`, the Share panel, the hero and footer links), and documentation (`README.md`, `docs/deployment.md`, `SECURITY.md`, `CLAUDE.md`, `docs/data-model.md`, `docs/go-to-market.md`, `HANDOFF.md`, `.env.example`). Suggested partition for future parallel work: `engine/` + `fixtures/` (finance), `ingest/` + `ai/` + `pipeline/` (platform), `apps/web/src/components/scene` (3D), everything else (product).

## Resume steps

1. `git status`; read this file, `CLAUDE.md`, and `docs/astra-review-response.md` (the deferred rows are the queue).
2. `make setup && make seed && make test && make eval`.
3. `make dev`, open `/demo` in two browsers and confirm each gets its own deal; walk the overview start section, the five sections (claims: "Confirm assessment" and "Ask about this claim"; financials: "What changed and why"; scenarios: the sticky result strip), the chat quick prompts, and the hero's three chapters with the pause control.
4. Register an account and read the verification link out of the API log (console emailer), open `/verify`, then `/forgot` and `/reset`; from the demo or your own deal open Share, invite a second account as a viewer, accept on `/invite` while signed in as that account, and confirm the viewer can read and chat but the upload and analysis actions are refused; open `/pilot` and confirm it reports checkout as not enabled without Stripe keys; check the chat footer's "of 300 answers used this month".
5. Walk the first-customer workflow once as a buyer would: upload, the progress strip, open a finding's source (the strip's evidence step turns done), export the seller questions (the questions step turns done), delete a document and accept the "Run analysis" suggestion, read the usage panel and `/trust`.
6. Next candidates, in order: run the 30-day plan in `docs/go-to-market.md` (ten conversations with the interview script, three supported pilots with the usability protocol, one paid pilot through `/pilot` with Stripe in test mode first, then live) and fill the release record and usability table in `docs/evaluation.md`; send one real email through Resend and one real test-mode checkout through Stripe from a deployment and note the result here; connect a free provider (`GEMINI_API_KEY` or `GROQ_API_KEY` in `.env`) and record live chat grounding and injection numbers; a per-deal permission field for the reviewed-work set; automatic retention and account deletion for non-demo accounts, and owner transfer of a shared deal; a shared rate limiter for multi-process deployments; reviewer comments UI (model exists); document version comparison; Anthropic-mode extraction run with recorded eval numbers; Lighthouse pass on the landing page.

## September 8, 2026: trust features

- **Correction loop** (`statement_corrections` table, migration c9e1a7b2d4f5; `POST /deals/{id}/financials/corrections`; `pipeline/analyze.apply_corrections`). A person corrects one mapped figure; the extracted row is never edited (invariant 2), the correction row keeps the original value, the deal is re-analysed inline with the correction applied, and the row stores an `impact` diff (metrics before/after, claim statuses moved, findings added/removed). Claims and reviewer decisions survive re-analysis; metrics, findings, and scenario results are recomputed. UI: Financial Verification, "Check what we read", "Correct a figure…".
- **Confidence budget** on `DealSummary.confidence` (decided / rules_only / needs_person / no_evidence / total), shown on the overview and the report header.
- **Review queue** (`GET /deals/{id}/review-queue`, page `/app/deals/[id]/inbox`), **answer trace** on every claim, **what would change this finding** (`reports/resolution.py`), **"Check what we read"** (`GET /deals/{id}/financials/mapping`), and the **messy demo** (`/demo?deal=messy`, `fixtures/messy.py`, `seed_messy`).
- Re-analysis rebuilds findings from the scenarios' *current* assumptions; tests that assert seeded finding text must not share a demo deal with tests that correct figures or edit scenarios (see `tests/test_corrections.py::_own_demo`).
- **Seller reply tracking** (`seller_replies`, migration d2f3b4c5e6a7; `GET/POST /deals/{id}/seller-replies`). The buyer records what the seller said to each exported question and reads it as answered / not answered / document requested; the latest reply per question wins; the Markdown export prints the reply under the question.
- **One-page PDF** (`GET /deals/{id}/summary.pdf`, `api/routes/summary_pdf.py`): three findings with "what would change this", the EBITDA bridge, DSCR by scenario, the confidence budget, coverage, corrections, and reply totals. Buttons on the overview and the report.
- **Revised documents** (`POST /deals/{id}/documents/{doc}/versions`, `GET .../diff`): a new version keeps the old file; the diff maps both statement versions and lists every changed figure with the calculations it moves and the claims that cite it (text documents diff by passage); the Deal Room offers "Revised copy" and "What changed" with a re-run button.
- **Keyboard review** in the queue: J / K move, Enter opens; the active row carries `aria-current`.
- **UX review of September 8 (Astra) acted on**: trace source links resolve the document from the evidence id (`document-viewer.tsx` ViewerBody); the calculation step reads as a worked example with labelled inputs; the chat panel no longer aborts its first send under StrictMode (deferred abort on real unmount) and adopts the server's thread after a failed first send; replies list the exact uncited passages and carry `stale` when a cited metric or evidence version is gone; scenario-name rule in the system prompt; mobile claim view leads with the comparison with Prev/Next and "Next unresolved"; Deal Room processing collapsed; "checked" wording everywhere; currency stated vs assumed (`detect_currency`); repeated claim issues merge into one seller question (19 for Northstar); covenant question wording; custom seller questions (`custom_questions`, migration e4a5b6c7d8f9) saved from chat after editing; chat context chips and expand-to-workspace; command palette (⌘K); "Since you were last here" digest from the audit trail (browser-remembered timestamp).
- **Notebook** (`review_notes`, migration f6b7c8d9e0a1; `GET/POST/DELETE /deals/{id}/notes`; page `/app/deals/[id]/notebook`): conclusions, assumptions, and open questions a person writes with the evidence and metrics attached (from a claim or a chat answer). A note that states a figure without a source is refused (422). Notes are the report's first section ("Reviewer's memo", role=reviewer) and the top of the one-page PDF.
- **Chat redesign**: the panel is "Chat" (was "Ask the deal"): one centred column, quiet citation pills, history hidden unless toggled or expanded, a rounded composer with the send button inside.
